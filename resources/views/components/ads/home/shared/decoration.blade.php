<div class="col-md-12 col-12 mb-3">
    <div class="form-group">
        <label for="decoration" class="form-label">{{ __('مفروشة/غير مفروشة') }}</label>
        <select name="decoration" id="decoration" class="form-select">
            <option value="{{ __('مفروشة') }}">{{ __('مفروشة') }}</option>
            <option value="{{ __('غير مفروشة') }}">{{ __('غير مفروشة') }}</option>
            <option value="{{ __('مفروش جزيئاً') }}">{{ __('مفروش جزيئاً') }}</option>
        </select>
    </div>
</div>
